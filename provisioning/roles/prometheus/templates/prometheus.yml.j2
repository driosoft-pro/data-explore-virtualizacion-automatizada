global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets:
{% for t in prometheus_targets_node_exporter %}
          - '{{ t }}'
{% endfor %}

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
